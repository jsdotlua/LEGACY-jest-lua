# This config exists so we can import the dependencies of all packages for analsis.
# THIS PACKAGE IS NOT DIRECTLY PUBLISHED!

[package]
name = "brooke/jest-lua"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

[dependencies]
LuauPolyfill = "core-packages/luau-polyfill@1.2.3"
Promise = "evaera/promise@4.0.0"
